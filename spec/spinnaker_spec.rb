# frozen_string_literal: true

RSpec.describe Spinnaker do
  it "has a version number" do
    expect(Spinnaker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
